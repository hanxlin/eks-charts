{{- if .Values.metadata.configFile }}
apiVersion: v1
kind: Secret
metadata:
    name: amundsen-metadata-config
    namespace: {{ .Release.Namespace }}
data:
  config.py: {{ .Values.metadata.configFile | b64enc | quote }}
{{- end }}
